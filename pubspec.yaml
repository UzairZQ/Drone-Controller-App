name: drone_controller_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  wifi_iot: ^0.5.2
  flutter_joystick: ^0.2.0  # For joystick controls
  flutter_spinkit: ^5.1.0  # For loading animations
  camera: ^0.11.0+1  # For camera feed
  permission_handler: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
